# yaml-language-server: $schema=https://raw.githubusercontent.com/microsoft/AgentSchema/refs/heads/main/schemas/v1.0/AgentManifest.yaml
name: human-in-the-loop-invocations
displayName: "Human In The Loop (Invocations)"
description: >
  A human-in-the-loop agent that demonstrates the approval-gate pattern using
  the azure-ai-agentserver-invocations SDK. Generates a proposal, pauses for
  human review, and resumes after the human approves, requests a revision, or
  rejects. Includes an inline OpenAPI spec for contract discovery.
metadata:
  tags:
    - AI Agent Hosting
    - Invocations Protocol
    - Bring Your Own
    - Human-in-the-Loop
    - OpenAPI
    - Python
template:
  name: human-in-the-loop-invocations
  kind: hosted
  protocols:
    - protocol: invocations
      version: 1.0.0
  environment_variables:
    # FOUNDRY_PROJECT_ENDPOINT and APPLICATIONINSIGHTS_CONNECTION_STRING
    # are injected by the platform (hosted) and translated by azd (local)
    # — do NOT declare them here.
    #
    # Model deployment name — resolved from the resources section below.
    - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
      value: "{{AZURE_AI_MODEL_DEPLOYMENT_NAME}}"
resources:
  - kind: model
    id: gpt-4.1-mini
    name: AZURE_AI_MODEL_DEPLOYMENT_NAME
